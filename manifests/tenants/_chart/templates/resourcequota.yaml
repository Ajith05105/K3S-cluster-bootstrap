apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{ .Values.tenantName }}-quota
  namespace: {{ .Values.tenantName }}
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "1Gi"
    limits.cpu: "2"
    limits.memory: "3Gi"
    pods: "10"
