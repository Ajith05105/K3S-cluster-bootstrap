apiVersion: v1
kind: ResourceQuota
metadata:
  name: alice-quota
  namespace: alice
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "1Gi"
    limits.cpu: "2"
    limits.memory: "3Gi"
    pods: "10"
